<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">

  <title><%- typeof title !== 'undefined' ? title : 'Home' %> | CSE 340 App</title>
  <meta name="description" content="CSE Motors — vehicles, upgrades, and reviews.">

  <!-- External stylesheet must use media='screen' as required -->
  <link rel="stylesheet" href="/css/styles.css" media="screen">
  <% if (locals.pageScripts && Array.isArray(locals.pageScripts) && locals.pageScripts.length) { %>
    <% locals.pageScripts.forEach(function (scriptPath) { %>
      <script src="<%= scriptPath %>" defer></script>
    <% }) %>
  <% } %>
</head>
